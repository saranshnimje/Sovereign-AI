# ============================================================
# Sovereign AI Workbench — Backend Dependencies
# All versions pinned for reproducibility.
# ============================================================

# --- Web Framework ---
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Schema validation (Pydantic v2) ---
pydantic==2.7.4
pydantic-settings==2.3.4

# --- Database ---
sqlalchemy==2.0.31
aiosqlite==0.20.0
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.2

# --- Authentication ---
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# --- HTTP client (Ollama, Qdrant calls) ---
httpx==0.27.0

# --- Vector database ---
qdrant-client==1.9.2

# --- System metrics ---
psutil==6.0.0

# --- Structured logging ---
structlog==24.2.0

# --- Docker SDK (sandbox) ---
docker==7.1.0

# --- Document processing (Phase 2) ---
boto3>=1.34.0,<2.0
PyMuPDF==1.24.5
python-docx==1.1.2
pandas==2.2.2
# Note: MIME detection uses pure-Python magic-bytes sniffer in utils/file_validator.py
# No libmagic dependency required — works cross-platform (Linux Docker + Windows dev)

# --- OCR (Phase 2 — optional, heavy install) ---
# Uncomment below only when OCR is needed; PaddleOCR requires ~2GB disk
# paddleocr==2.7.3
# paddlepaddle==2.6.1

# --- Dev/test ---
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
pyyaml==6.0.1
